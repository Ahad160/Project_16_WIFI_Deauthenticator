#!/usr/bin/env python

# Note: This script runs defite from within a cloned git repo.
# The script `bin/defite` is designed to be run after installing (from /usr/sbin), not from the cwd.

from defite import __main__
__main__.entry_point()
